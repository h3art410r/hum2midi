# 项目协作说明

## 文档约定
- 项目文档、产品规格、决策记录和开发说明统一放在 `docs/` 目录。新增文档不要放在仓库根目录。
- 当前产品范围以 `docs/SPEC.md` 为准；若发现实现与 spec 不一致，先记录并报告，不要默默扩大范围。

## 产品方向
- 本项目当前是一个最小端到端 Demo，目标是把用户真实哼唱转换成两个可对比的音乐版本。它不是生产级产品。
- 手机浏览器是主要体验和验收目标；电脑浏览器需要提供 16:9 调试视图。
- 当前不实现短视频生成、账号、社区、支付、作品云端历史、MIDI/stems 导出或 DAW 编辑。

## 技术约束
- 后端使用 Python。优先保持简单，避免为 Demo 引入不必要的服务、数据库或抽象层。
- 哼唱音频理解必须调用云端多模态模型；禁止本地模型推理，也禁止在云模型不可用时静默切换到本地模型。
- MIDI 到音频允许使用确定性的程序化合成或音频处理；这不是模型推理，不能代替云端旋律识别。
- 云模型凭据只能从环境变量或安全的运行时配置读取；不得提交密钥或将其写入文档、日志和前端代码。
- 模型供应商调用应通过小而清晰的服务接口隔离，便于替换；不提前建设通用多模型平台。
- 真实录音必须进入实际转换流程。演示数据、固定音频或 mock 输出不得伪装成实时生成结果。

## 实施与沟通
- 先查阅 `docs/SPEC.md` 和现有实现，再改动代码。围绕可现场演示的最短闭环工作。
- 对会影响核心体验、模型效果、云服务选择、成本或数据处理方式的未决问题，整理具体选项与影响并交由项目负责人决定；其他低风险细节自行选择并记录。
- 报告问题时提供可复现步骤、预期/实际结果和影响范围；不要只报告抽象结论。
